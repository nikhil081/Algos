#include <iostream>
#include<bits/stdc++.h>
#include<string.h>
using namespace std;

int main()
{
    string str;
    int countt=0;
    getline(cin,str);
   for(int i=0;i<str.length();i++){
        if(str[i]== ' '){
            ++countt;
        }
    }
    cout<<countt+1;

    return 0;
}
